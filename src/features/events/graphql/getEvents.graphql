query GetEvents($first: Int = 10) {
  events(first: $first) {
    nodes {
      id
      databaseId
      title
      excerpt
      featuredImage {
        node {
          sourceUrl
          altText
        }
      }
      eventsFields {
        date
        time
        location
        slots
        price
      }
    }
  }
}
